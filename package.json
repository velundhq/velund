{
  "name": "@velund/workspace",
  "private": "true",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "pnpm -r build",
    "build:vite": "pnpm run -F velund build",
    "build:core": "pnpm run -F @velund/core build",
    "build:example": "pnpm run -F @velund/example build",
    "dev:example": "pnpm run -F @velund/example dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "me@dantezz.ru",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.9.2"
  }
}
